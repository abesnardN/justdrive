{% extends 'base.html.twig' %}

{% block title %}Etatvehicule index{% endblock %}

{% block body %}
    <h1>Etatvehicule index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Idetat</th>
                <th>Libelle</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for etatvehicule in etatvehicules %}
            <tr>
                <td>{{ etatvehicule.idetat }}</td>
                <td>{{ etatvehicule.libelle }}</td>
                <td>
                    <a href="{{ path('etatvehicule_show', {'idetat': etatvehicule.idetat}) }}">show</a>
                    <a href="{{ path('etatvehicule_edit', {'idetat': etatvehicule.idetat}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('etatvehicule_new') }}">Create new</a>
{% endblock %}
