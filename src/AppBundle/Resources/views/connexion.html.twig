{% extends 'base.html.twig' %}
{% block title %}Connectez vous !{% endblock %}
{% block body %}

{{ form_start(form) }}
    {{ form_errors(form) }}
    <div class="form-group">
        {{ form_row(form.mail) }}
    </div>
    <div class="form-group">
        {{ form_row(form.password) }}
    </div>

    <a href="{{ path('demande_index') }}" class="btn btn-info" role="button">Se connecter</a>
{{ form_end(form) }}
{% endblock %}