<form method="post" action="{{ path('adresse_delete', {'idadresse': adresse.idadresse}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ adresse.idadresse) }}">
    <button class="btn">Delete</button>
</form>
