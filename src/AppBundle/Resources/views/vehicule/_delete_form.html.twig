<form method="post" action="{{ path('vehicule_delete', {'idvehicule': vehicule.idvehicule}) }}" onsubmit="Etes vous sûr de vouloir supprimer ce véhicule ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vehicule.idvehicule) }}">
    <button class="btn btn-danger">Supprimer le véhicule</button>
</form>