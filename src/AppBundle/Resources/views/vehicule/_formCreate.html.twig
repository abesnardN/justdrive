{{ form_start(form) }}
<div class="container">
    <div class="card">
        <div class="card-body">
            <h1 class="card-title">Création d'un véhicule</h1>
                <div class="card">
                    <div class="card-body">
                        <div class="form-group">
                            {{ form_row(form.immatriculation) }}
                        </div>
                        <div class="form-group">
                            {{ form_row(form.modele) }}
                        </div>
                        <div class="form-group">
                            {{ form_row(form.marque) }}
                        </div>
                        <div class="form-group">
                            {{ form_row(form.nbplace) }}
                        </div>
                        <div class="form-group">
                                {{ form_row(form.kilometrage) }}
                            </div>
                        <div class="form-group">
                            {{ form_row(form.datepremierecirculation) }}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="row text-center mt-3">
    <div class="col-sm">
        {{ form_row(form.save) }}
    </div>

</div>
{{ form_end(form) }}
